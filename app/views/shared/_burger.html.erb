<nav role="navigation">
  <div id="menuToggle">
    <!--
    A fake / hidden checkbox is used as click reciever,
    so you can use the :checked selector on it.
    -->
    <input type="checkbox" id="burger-checkbox" />

    <!--
    Some spans to act as a hamburger.
    -->
    <div class="burger-span">
      <span></span>
      <span></span>
      <span></span>
    </div>
    <!--
    Too bad the menu has to be inside of the button
    but hey, it's pure CSS magic.
    -->
    <ul id="menu">
      <li class="nav-item active">
        <%= link_to "", root_path, class: "nav-link fa-solid fa-solid fa-house fa-2xl" %>
      </li>
      <% if user_signed_in? && current_user.is_doctor == false %>
      <li class="nav-item active">
        <%= link_to "", diaries_path, class: "nav-link fa-solid fa-chart-line fa-2xl" %>
      </li>
      <% end %>
      <li class="nav-item active">
      <% if user_signed_in? && current_user.is_doctor == false %>
        <%= link_to "", edit_user_path(current_user), class: "nav-link fa-solid fa-user fa-2xl" %>
      <% elsif user_signed_in? && current_user.is_doctor == true %>
        <%= link_to "", destroy_user_session_path, class: "nav-link fa-solid fa-user fa-2xl", data: { turbo_method: :delete, turbo_confirm: "Voulez-vous vous déconnecter ?" } %>
      <% else %>
        <%= link_to "", user_session_path, class: "nav-link fa-solid fa-user fa-2xl" %>
      <% end %>
      </li>
    </ul>
  </div>
</nav>
